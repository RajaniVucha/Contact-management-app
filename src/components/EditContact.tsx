import React, { ReactElement } from 'react'

interface Props {
    
}

function EditContact({}: Props): ReactElement {
    return (
        <div>
            EDDIT
        </div>
    )
}

export default EditContact
